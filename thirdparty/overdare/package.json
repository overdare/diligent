{
  "name": "@overdare/workspace",
  "private": true,
  "diligent": {
    "projectName": "OVERDARE Agent"
  },
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "install:local": "bun run scripts/install.ts",
    "deploy": "bun run scripts/deploy.ts",
    "typecheck": "NO_COLOR=1 FORCE_COLOR=0 bunx tsc --pretty false --noEmit -p plugins/plugin-studiorpc/tsconfig.json && NO_COLOR=1 FORCE_COLOR=0 bunx tsc --pretty false --noEmit -p plugins/plugin-rag/tsconfig.json && NO_COLOR=1 FORCE_COLOR=0 bunx tsc --pretty false --noEmit -p plugins/plugin-validator/tsconfig.json"
  }
}
