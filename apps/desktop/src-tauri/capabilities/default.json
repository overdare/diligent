{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl-manifests.d.ts",
  "identifier": "default",
  "description": "Default capabilities for Diligent desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-kill",
    "process:allow-exit"
  ]
}
