# Debug Viewer

React web viewer for inspecting Diligent session logs.

```
src/
  client/          React app (App.tsx, components, hooks, lib)
  server/          API server, session parser, file watcher
  shared/          Shared protocol and types
index.html         Entry HTML
vite.config.ts     Vite build config
test/              Unit tests
```
